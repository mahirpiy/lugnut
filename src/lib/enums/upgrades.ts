export enum SubscriptionType {
  MONTHLY = "monthly",
  YEARLY = "yearly",
  PERMANENT = "permanent",
}

export enum SubscriptionStatus {
  ACTIVE = "active",
  EXPIRED = "expired",
  CANCELED = "canceled",
}

export enum UsageCreditType {
  VEHICLE = "vehicle",
  JOB = "job",
  FUEL = "fuel",
  ODOMETER = "odometer",
}
